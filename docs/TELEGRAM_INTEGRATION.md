# Интеграция "Лестницы Союза" в Telegram (Telegram Mini App)

Самый простой, надежный и современный способ запустить ваш тест в Telegram — это использовать технологию **Telegram Mini Apps (TMA)**.

## Почему это лучшее решение?

1.  **Не нужно переписывать код:** Мы используем текущее React-приложение как есть.
2.  **Сохранение функционала:** Все анимации, мандалы, сложные подсчеты и графики работают внутри Telegram.
3.  **Нативный опыт:** Приложение открывается прямо внутри чата, выглядит как часть Telegram.
4.  **Простота обновления:** Вы обновляете сайт, и бот обновляется автоматически.

---

## Пошаговая инструкция

### Шаг 1: Подготовка приложения (Опционально, но рекомендуется)

Чтобы приложение чувствовало себя "родным" в Telegram, можно добавить небольшую интеграцию для получения темы оформления пользователя и его имени.

1.  Установите SDK:
    ```bash
    npm install @twa-dev/sdk
    ```

2.  Инициализируйте SDK в `src/main.tsx` (или `src/App.tsx`):
    ```typescript
    import WebApp from '@twa-dev/sdk'

    // Инициализация при старте
    WebApp.ready();
    // Расширяем на весь экран
    WebApp.expand();
    ```

### Шаг 2: Деплой (Публикация в интернет)

Telegram Mini App требует, чтобы ваше приложение было доступно по **HTTPS**.
Самый простой способ — использовать **Vercel** (бесплатно и быстро).

1.  Зарегистрируйтесь на [vercel.com](https://vercel.com).
2.  Установите Vercel CLI: `npm i -g vercel`
3.  В папке проекта выполните команду:
    ```bash
    vercel
    ```
4.  Следуйте инструкциям (соглашайтесь со всем по умолчанию).
5.  В конце вы получите ссылку вида: `https://12union-test.vercel.app`

### Шаг 3: Создание бота в Telegram

1.  Откройте в Telegram бота **@BotFather**.
2.  Отправьте команду `/newbot`.
3.  Придумайте имя (например, `UnionLadderTest`) и юзернейм (например, `UnionLadderBot`).
4.  Вы получите **API Token** (сохраните его, если планируете делать рассылки, но для запуска Web App он пока не критичен).

### Шаг 4: Подключение Web App к боту

В том же **@BotFather**:

1.  Отправьте команду `/mybots`.
2.  Выберите вашего бота.
3.  Выберите **Bot Settings** -> **Menu Button** -> **Configure Menu Button**.
4.  Отправьте ссылку, которую вы получили на Шаге 2 (например, `https://12union-test.vercel.app`).
5.  Введите текст кнопки, например "Пройти тест".

### Готово! 🚀

Теперь любой пользователь, открывший вашего бота, увидит кнопку "Пройти тест" (слева от поля ввода или в меню). При нажатии откроется ваше приложение прямо внутри Telegram.

---

## Продвинутые возможности (на будущее)

Если вы захотите сохранять результаты пользователей в базу данных, привязанную к их Telegram ID:

1.  В приложении через `WebApp.initDataUnsafe.user` вы можете получить `id`, `first_name`, `username` пользователя.
2.  Отправляйте эти данные вместе с результатами теста на ваш сервер.

## Резюме

Вам **не нужно** переписывать тест на языке кнопок и текстовых сообщений Telegram. Это было бы шагом назад. Используйте Mini App — это стандарт индустрии для сложных интерфейсов в Telegram.
