# 🎨 Дизайн-система "Лестница Союза"

Современная дизайн-система для проекта "12union", обеспечивающая единообразный и привлекательный интерфейс.

## 📁 Структура файлов

- `design-system.css` - Основная дизайн-система с CSS переменными
- `index.css` - Существующие стили (сохранены для совместимости)
- `shared-components.css` - Общие компоненты

## 🎯 Ключевые особенности

### ✨ Современный дизайн
- Мягкие градиенты и тени
- Скругленные углы и плавные анимации
- Современная типографика с Inter шрифтом
- Семантическая цветовая палитра

### 📱 Мобильная адаптация
- Responsive дизайн "mobile-first"
- Адаптивные сетки и отступы
- Оптимизированные размеры для сенсорных экранов

### 🎨 Цветовая система

#### Основная палитра
```css
--primary-50 до --primary-900  /* Серые оттенки */
--accent-blue, --accent-purple, --accent-green, etc. /* Акцентные цвета */
```

#### Семантические цвета
```css
--success, --warning, --error, --info /* С соответствующими светлыми версиями */
```

### 📐 Spacing & Layout
Единая система отступов от `--space-1` (4px) до `--space-16` (64px)

### 🔄 Радиусы скругления
От `--radius-sm` (6px) до `--radius-3xl` (32px)

### 🌟 Тени
Пять уровней теней от `--shadow-sm` до `--shadow-2xl`

## 🧩 Компоненты

### ModernSection
Основной контейнер для секций с поддержкой:
- Сворачивания/разворачивания
- Вариантов дизайна (primary, success, warning, purple)
- Приоритетов (high, medium, low)
- Иконок и подзаголовков

```tsx
<ModernSection
  title="Заголовок"
  subtitle="Подзаголовок"
  icon={<Brain className="w-5 h-5" />}
  variant="primary"
  priority="high"
>
  Контент секции
</ModernSection>
```

### MetricCard
Карточки для отображения ключевых метрик:

```tsx
<MetricCard
  title="Личная зрелость"
  value="7 уровень"
  subtitle="Описание"
  color="blue"
  size="lg"
  icon={<User className="w-6 h-6" />}
  trend="up"
/>
```

### InsightCard
Карточки для инсайтов и рекомендаций:

```tsx
<InsightCard
  title="Заголовок"
  content="Содержание инсайта"
  type="insight" // insight, recommendation, warning, tip
  compact={false}
/>
```

### ModernTabs
Современные табы для группировки контента:

```tsx
<ModernTabs 
  tabs={[
    { id: 'tab1', label: 'Таб 1', content: <div>Контент</div> }
  ]}
  defaultTab="tab1"
/>
```

### QuickStats
Компактная панель быстрой статистики:

```tsx
<QuickStats stats={[
  { label: 'Метрика', value: '100%', color: 'blue' }
]} />
```

### ProgressIndicator
Индикаторы прогресса:

```tsx
<ProgressIndicator
  label="Прогресс"
  value={75}
  color="blue"
  showValue={true}
/>
```

### ToggleView
Переключатель видимости контента:

```tsx
<ToggleView label="Показать детали">
  <div>Скрытый контент</div>
</ToggleView>
```

## 🎭 Утилитарные классы

### Карточки
```css
.modern-card /* Базовый стиль современной карточки */
.modern-card--primary, --success, --warning, --purple /* Варианты */
```

### Типографика
```css
.heading-1, .heading-2, .heading-3 /* Заголовки */
.text-primary, .text-secondary, .text-muted /* Цвета текста */
```

### Бейджи
```css
.badge, .badge--primary, --success, --warning, --purple
```

### Анимации
```css
.animate-fade-in-up, .animate-pulse, .animate-slide-in-right
```

## 🌓 Dark Mode
Полная поддержка темного режима через CSS переменные

## 📏 Breakpoints
- sm: 640px
- md: 768px  
- lg: 1024px
- xl: 1280px

## 🎯 Принципы использования

1. **Консистентность** - Всегда используйте CSS переменные вместо хардкода
2. **Семантичность** - Используйте семантические цвета для соответствующих состояний
3. **Адаптивность** - Всегда тестируйте на мобильных устройствах
4. **Производительность** - Используйте CSS переменные для лучшей производительности
5. **Доступность** - Соблюдайте контрастность и семантику

## 🚀 Быстрый старт

1. Импортируйте стили:
```css
@import './styles/design-system.css';
```

2. Используйте компоненты:
```tsx
import { ModernSection, MetricCard } from '../modern/ModernResultsLayout';
```

3. Применяйте утилитарные классы:
```tsx
<div className="modern-card modern-card--primary">
  <h2 className="heading-2">Заголовок</h2>
</div>
```

## 🔧 Кастомизация

Изменяйте CSS переменные в `:root` для глобальной кастомизации цветов, отступов и других параметров.

---

*Дизайн-система создана с учетом современных тенденций UX/UI и психологических принципов восприятия интерфейсов.* 